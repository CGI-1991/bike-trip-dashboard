{
  "name": "bike-trip-dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:practical": "node scripts/generate-practical-data.mjs",
    "check:rga-trip-package": "node scripts/generate-rga-trip-package.mjs --check",
    "generate:rga-golden": "node scripts/generate-rga-golden.mjs",
    "check:rga-golden": "node scripts/generate-rga-golden.mjs --check",
    "prebuild": "npm run generate:practical && npm run check:rga-trip-package && npm run check:rga-golden",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.21",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
